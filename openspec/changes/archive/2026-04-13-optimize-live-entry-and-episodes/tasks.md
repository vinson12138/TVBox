# 任务：优化主页直播入口与详情页选集体验

> **状态说明（2026-04-12 核查）**
>
> 以下任务均尚未在代码中实现。唯一相关的已有代码变更来自上游提交
> `baddbd15b Fix episode compare`：`Episode.equals()` 改为同时对比 name + url，
> 并新增了 `Episode.matchesName()` 方法；但这两项不属于下列正式任务范围。

## 1. 主页直播入口开关（决策 1）

- [x] 1.1 在 `Setting.java` 中添加 `isHomeLiveVisible()` 方法，默认返回 `true`
- [x] 1.2 在 `Setting.java` 中添加 `putHomeLiveVisible(boolean)` 方法
- [x] 1.3 手机端 `HomeActivity`：将直播导航项可见性改为 `LiveConfig.hasUrl() && Setting.isHomeLiveVisible()`
- [x] 1.4 电视端 `HomeActivity`：将直播功能区项目添加条件改为 `LiveConfig.hasUrl() && Setting.isHomeLiveVisible()`
- [x] 1.5 手机端 `SettingFragment`：添加"首页显示直播入口"开关 UI 及点击逻辑
- [x] 1.6 电视端 `SettingActivity`：添加"首页显示直播入口"开关 UI 及点击逻辑
- [x] 1.7 在 `strings.xml`（含 zh-rCN、zh-rTW）中添加对应的字符串资源
- [x] 1.8 在手机端与电视端的设置布局 XML 中添加开关控件

## 2. Episode 结构化识别（决策 6 基础）

- [x] 2.1 为 `Episode` 添加 `contentType` 字段（正片 / 剧场版 / 特别篇 / 花絮 / 预告）
- [x] 2.2 实现 `parseContentType()`：按关键词（OVA、SP、预告、花絮、剧场版等）识别内容类型
- [x] 2.3 为 `Episode` 添加 `versionKey` 字段，识别清晰度、编码、语言、来源等版本标签
- [x] 2.4 实现 `getDisplayText()`：剧集类正片且识别出可信主序号时返回纯数字，否则返回原始名称
- [x] 2.5 保留原始 `name` 不变；`getDisplayText()` 仅影响展示，不影响历史匹配（当前代码已满足，无需变更）

## 3. Flag 分组与排序（决策 3 / 5 / 9 基础）

- [x] 3.1 在 `Flag` 中实现 `getGroups()`：按 `contentType` 将 episodes 分入不同 `EpisodeGroup`
- [x] 3.2 分组优先级顺序：正片 → 剧场版 → 特别篇 → 花絮 → 预告
- [x] 3.3 组内排序：按 `contentType 优先级 → 季 → 主序号 → 版本优先级 → 原始顺序`
- [x] 3.4 实现 `EpisodeGroup` bean：包含分组名称、分组类型、episodes 列表
- [x] 3.5 实现保守去重：内容键 + 版本键相同才去重，不同版本标签的同序号集不误合并
- [x] 3.6 分页尺寸计算：正片分组每页 20 集，其他分组每页 10 个

## 4. 倒序逻辑修正（决策 7 / 8）

- [x] 4.1 倒序作用范围收窄为当前选中分组的主序号序列，不对整个 episodes 列表 `reverse()`
- [x] 4.2 同一主序号下的多版本顺序在倒序时保持稳定（EpisodeGroup 内部排序保证稳定性）
- [x] 4.3 移除箭头式倒序按钮：手机端两套布局 XML 中已将 `AppCompatImageView` 替换为文字按钮 `MaterialTextView`
- [x] 4.4 带文字的正序/倒序按钮切换后立即同步更新按钮文本（文本反映当前状态）

## 5. 电视端分组条（决策 5）

- [x] 5.1 在 `EpisodeGroupPresenter` 中实现分组条 item 展示逻辑
- [x] 5.2 电视端 `VideoActivity`：在选集区上方添加分组条，仅在分组数 > 1 时可见
- [x] 5.3 焦点顺序更新为：`flag → quality → group → episode → array → part → quick`
- [x] 5.4 切换分组时：选集条从该分组首集开始，分页条同步重置
- [x] 5.5 分页条继续保留，但只反映当前分组内的序列范围
- [x] 5.6 更新电视端详情页布局 XML，添加分组条所需视图

## 6. 手机端选集网格（决策 4）

- [x] 6.1 手机端 `VideoActivity`：主选集区改为横向流动 `GridLayoutManager`（竖向多行）
- [x] 6.2 选集区上方添加分组切换条，仅在分组数 > 1 时显示
- [x] 6.3 "更多选集"入口保留，内容改为展示当前分组的集数
- [x] 6.4 `EpisodeGroupAdapter` 实现分组条点击切换逻辑
- [x] 6.5 更新手机端详情页布局 XML（含 sw600dp 变体）
- [x] 6.6 单行模式下，集数文本在按钮内水平居中
- [x] 6.7 当任一选集存在副标题时，整组按钮统一保持双行高度
- [x] 6.8 手机端“更多选集”弹窗与主视图统一展示口径（标题、副标题、二级标签、宽度规则）

## 7. 历史续播兼容

- [x] 7.1 历史匹配优先级：`episodeUrl` 精确匹配 → 结构化主序号 + 内容类型 → 名称近似 → 分组首项
- [x] 7.2 现有 `History.findEpisode()` 中补充 URL 优先匹配逻辑
- [x] 7.3 切换分组时，定位逻辑优先匹配当前分组内已有历史位置（setGroupActivated 中 scroll to activated episode）
- [x] 7.4 已有观看历史的视频默认倒序；未观看的视频默认正序

## 8. 范围分页与定位

- [x] 8.1 为当前激活分组引入统一的范围页模型（标题、起止索引、定位锚点）
- [x] 8.2 正片分组按 20 集分页，其他分组按 10 个分页
- [x] 8.3 正序范围文案调整为如 `1~20`、`21~24`
- [x] 8.4 倒序范围文案调整为如 `24~21`、`20~1`
- [x] 8.5 切换范围时，定位到该范围的第一个按钮
- [x] 8.6 电视端范围条跳转逻辑改为基于页模型，而不是固定 `(position - 2) * 20`
- [x] 8.7 手机端“更多选集”弹窗分页规则与主范围口径保持一致

## 9. 视频页 Part 组件收敛

- [x] 9.1 电视端视频页默认不显示 `part` 组件
- [x] 9.2 更新焦点顺序，移除 `part` 后保持 `flag → quality → group → episode → array → quick`

## 10. 验证

- [ ] 10.1 短剧（< 12 集）：分页正常，倒序不乱
- [ ] 10.2 长剧（50 集以上）：分页每页 20，倒序作用于当前分组
- [ ] 10.3 动漫（含 OVA / SP / 剧场版）：正片与特别篇各自独立分组
- [ ] 10.4 同集多清晰度：不同版本归并到同一集下，不误去重
- [ ] 10.5 首页直播入口：可在设置中关闭，手机端与电视端同步生效
- [ ] 10.6 续播命中：历史记录在整理后仍能正确定位到上次播放集
- [ ] 10.7 单行集数按钮：文本居中且宽度按组内最大集数统一
- [ ] 10.8 存在副标题时：整组按钮高度一致，单行项使用占位不跳高
- [ ] 10.9 24 集场景：正序显示 `1~20`、`21~24`；倒序显示 `24~21`、`20~1`
- [ ] 10.10 范围切换后：手机端与电视端都定位到该范围首项
- [ ] 10.11 视频页 `part` 组件不再显示，焦点路径与布局无残留异常
- [x] 10.12 编译验证：`compileLeanbackArm64_v8aDebugJavaWithJavac` 和 `compileMobileArm64_v8aDebugJavaWithJavac` 通过
